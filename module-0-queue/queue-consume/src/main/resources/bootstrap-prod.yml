
server.port: 9871
logging.config: classpath:log-prod.xml

spring:
  application.name: queue-consume
  profiles.active: prod

spring.cloud.config:
  uri: http://prod_config:8001/
  name: all,queue
  profile: ${spring.profiles.active}

management.security.enabled: false
